import { Size } from '../types';
declare type NodeSide = 'NORTH' | 'SOUTH' | 'EAST' | 'WEST';
export declare type PortData = {
    y: number;
    x: number;
    side: NodeSide;
} & Size;
export declare type Preset = (nodeId: string) => (null | {
    port(data: {
        nodeId: string;
        side: 'input' | 'output';
        key: string;
        index: number;
        ports: number;
    } & Size): PortData;
    options?: (id: string) => Record<string, string | number | boolean>;
});
export {};
//# sourceMappingURL=types.d.ts.map